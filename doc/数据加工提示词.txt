你现在是一名资深数据分析工程师和数据仓库架构师。
我正在完成一个“大数据专题：数据中心运行监控大屏”课程项目。

现在需要帮助我完成一个完整的数据中心运行监控大屏项目的数据准备阶段。
本阶段包括：
① Docker环境部署
② MySQL数据库设计
③ 原始dat文件导入
④ SQL数据清洗
⑤ SQL统计分析
⑥ 生成可供可视化大屏直接使用的数据。

老师提供了4个原始明细数据文件：
1. host_detail.dat
   - 存储服务器基础信息，例如主机ID、主机名称、机房等。
2. mod_detail.dat
   - 存储监控指标字典，例如CPU使用率、内存使用量、网络流量、磁盘指标等。
3. pref_tsar.dat
   - 存储服务器性能监控数据，包括时间戳、主机ID、指标名称、指标值。
   - 主要涉及CPU、内存、负载、网络、进程等指标。
4. disk_tsar.dat
   - 存储磁盘性能监控数据，包括磁盘读写、IO等待、利用率等指标。

项目目标：
基于以上四个原始明细文件：
原始数据(dat)
        ↓
导入MySQL数据库
        ↓
SQL数据清洗和加工
        ↓
生成统计分析结果
        ↓
为数据可视化大屏提供数据
请你按照企业数据仓库项目的方式设计完整的数据加工方案。


请完成以下任务：


任务0：
请先设计整个数据准备流程。
要求：
从Docker开始。
包括：
Docker
↓
MySQL容器部署
↓
数据库创建
↓
数据表设计
↓
dat导入
↓
数据清洗
↓
统计分析
↓
生成结果表
↓
供前端调用


任务1：
读取并分析四个dat文件。

获取：
1. 每个文件的数据规模；
2. 字段名称；
3. 字段含义；
4. 数据类型；
5. 数据之间的关联关系。

请判断：
host_detail、mod_detail、pref_tsar、disk_tsar之间应该如何关联。
应该建立几张MySQL表？
哪些属于维度表？
哪些属于事实表？
是否需要建立统计结果表？
是否需要建立中间表？
请按照数据仓库思想设计。


任务2：
根据原始数据设计MySQL数据库。
要求：
1. 给出CREATE TABLE语句。

包括：
- 原始明细表
- 时间转换后的字段
- 必要索引设计

要求考虑：
- timestamp毫秒转换
- value字段类型转换
- 查询效率
- 后续统计需求

输出完整SQL。


任务3：
请根据“数据中心运行监控大屏”的业务场景，自主设计需要统计分析的指标。
不要只完成简单的时间转换。
请从数据中心运维监控角度思考：
一个企业监控大屏通常需要展示哪些内容？

请设计：
A. 首页核心指标(KPI)
例如：
- 服务器总数量
- 在线服务器数量
- CPU平均使用率
- 内存平均使用率
- 网络流量
- 磁盘IO状态

B. 趋势分析
例如：
- CPU一天24小时变化趋势
- 内存一天变化趋势
- 网络流量趋势
- 磁盘读写趋势

C. 排名分析
例如：
- CPU使用率最高TOP10服务器
- 内存占用最高TOP10服务器
- 磁盘IO最高TOP10服务器

D. 分布分析
例如：
- 不同机房服务器数量
- 不同指标占比
- 主机资源状态分布

E. 异常分析
例如：
- CPU超过80%的服务器数量
- 内存超过阈值服务器
- 高IO磁盘数量

请结合实际字段判断哪些指标可以实现。


任务4：
针对最终确定的大屏指标：
设计整个数据库，为每一个指标编写SQL。
要求：
必须基于原始四张表。
必须体现：
- 时间戳解析
- 日期小时拆分
- GROUP BY聚合
- AVG平均值
- MAX最大值
- MIN最小值
- COUNT统计
- JOIN关联

每个SQL说明：
1. 作用
2. 输入表
3. 输出字段
4. 前端图表用途


任务5：
设计最终提供给前端的数据表。之后Vue/Echart要用。
不要直接让前端查询原始表。
请设计数据服务层。
例如：
dashboard_cpu_hour
dashboard_host_rank
dashboard_room_summary
dashboard_disk_status

要求：
每张统计结果表说明：
- 表名
- 字段
- 字段含义
- 示例数据
- 对应的大屏组件


任务6：
请输出最终项目数据处理阶段应该产生的文件。
包括：
1. 数据库建表SQL
2. 数据导入SQL
3. 数据清洗SQL
4. 数据统计SQL
5. 结果表设计文档

请按照真实企业项目交付标准整理。