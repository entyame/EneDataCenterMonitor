你现在已经完成数据中心运行监控大屏项目的数据层开发。
下一阶段需要完成完整可运行的Web可视化系统。
请你作为资深全栈工程师，设计后续开发方案。
目标：
开发数据中心运行监控大屏，可以在网页端显示。
技术要求：
前端：
Vue3 + Vite
ECharts
Axios
后端：
Node.js + Express
数据库：
MySQL 8.0
请输出：
1. 推荐项目目录结构
2. 前后端如何通信
3. 需要设计哪些API接口
4. 每个接口对应查询哪些dashboard结果表
5. 大屏需要展示哪些模块
6. 每个模块对应什么图表类型
不要写代码，先完成架构设计。


根据刚才设计的数据中心监控系统架构。

现在开始开发后端。
技术：
Node.js
Express
mysql2
要求：
1. 创建backend目录
2. 实现MySQL连接池
3. 创建REST API接口

接口至少包括：
/api/kpi
返回：
- 主机数量
- 指标数量
- CPU平均值
- 内存使用情况
/api/cpu/hour
返回CPU小时趋势
/api/memory/hour
返回内存趋势
/api/network/hour
返回网络流量趋势
/api/load/hour
返回负载趋势
/api/disk/hour
返回磁盘趋势
/api/hosts/rank
返回主机性能排行
/api/rooms
返回机房统计
/api/alerts
返回异常信息
要求：
所有接口直接查询dashboard结果表。
输出：
完整代码。
包括：
package.json
server.js
数据库配置
routes
controllers

后端API完成后，
请验证所有接口并告诉我结果。
如果可以也可以（在文档里）说明一下，
要求：
1. 启动命令
2. 测试curl命令
3. 浏览器访问方式
4. 每个接口正常返回的数据格式示例
5. 常见错误排查方法